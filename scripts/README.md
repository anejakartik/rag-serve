# Recording demo

Install `asciinema` and `asciinema2gif` (optional) to record demo sessions.

Example:

```bash
./scripts/record_demo.sh rag-demo.cast
asciinema2gif rag-demo.cast rag-demo.gif
```

Copy `rag-demo.gif` into `public/demo.gif` and update README.
